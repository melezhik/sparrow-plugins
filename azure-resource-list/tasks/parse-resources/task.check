generator: <<CODE
!perl6

  my @list = config()<list> || ();

  say "note: ========================= [ check ] ========================== ";

  if @list {
    for @list -> $s {
      say "regexp: ^^ '[$s]'";
    }
  }

CODE

