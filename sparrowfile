#!perl6

bash "uptime";

say "os: {os()}";

task-run "install nginx", "package-generic", %(
    list => %(
      debian => (
        'nginx',
      ),
      centos => (
        'epel-release', 'nginx'
      )
    )
);

service-enable "nginx";
service-restart "nginx";
